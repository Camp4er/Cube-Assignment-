import React from 'react';
import Outer from './components/Outer/Outer';

const App: React.FC = () => {

    return (
        <div className="app">
            <Outer/>
        </div>
    );
};

export default App;
